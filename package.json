{
  "name": "intro-to-agents",
  "private": true,
  "description": "Slides and runnable code on building agents with Pydantic AI.",
  "scripts": {
    "build": "marp --input-dir docs --output _site --html --allow-local-files",
    "watch": "marp --input-dir docs --output _site --html --allow-local-files --server",
    "preview": "marp --input-dir docs --html --allow-local-files --preview",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.3.1"
  }
}
